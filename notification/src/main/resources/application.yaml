ktor:
  application:
    modules:
      - com.redwater.ApplicationKt.notificationModule
  deployment:
    port: 8086
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
  secret: $JWT_SECRET